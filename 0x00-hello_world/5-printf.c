#include <stdio.h>

/**
 * Description: main - entry string to stdout
 *
 * Return: 0 (if successful)
 */
int main(void)
{
printf("%s\n", "with proper grammar, but the outcome is a piece of art,");
return (0);

}

